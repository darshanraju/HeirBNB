import React, { Component } from 'react';
import Typography from '@material-ui/core/Typography';

class HomePage extends Component {
  render () {
    return (
      <React.Fragment>
        <Typography>Accomodation App</Typography>
      </React.Fragment>
    )
  }
}

export default HomePage;